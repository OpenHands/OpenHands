/**
 * Legacy sidebar version tile.
 *
 * Heimdall fork: intentionally a no-op. Upstream OpenHands version checks
 * are disabled; interesting changes are ported on request.
 */
export function AgentCanvasVersionTile(
  _props: {
    className?: string;
    hideWhenUpToDate?: boolean;
  } = {},
) {
  return null;
}
