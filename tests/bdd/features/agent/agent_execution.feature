# Agent execution and step lifecycle tests
Feature: Agent executes steps in response to user input
  In order to process user tasks
  As an agent
  I want to execute steps, call LLM, and invoke tools

  # Scenario templates go here
  # Example:
  # Scenario: User asks agent to list files
  #   Given an agent session is running
  #   When the user sends "list files in src/"
  #   Then the agent receives the message
  #   And the LLM is called with the conversation history
  #   And a "run" action is created
