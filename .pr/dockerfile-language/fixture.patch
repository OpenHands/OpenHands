diff --git a/src/fixtures/canvas-demo-conversation.ts b/src/fixtures/canvas-demo-conversation.ts
index 59f0f7f..b5ca469 100644
--- a/src/fixtures/canvas-demo-conversation.ts
+++ b/src/fixtures/canvas-demo-conversation.ts
@@ -13,27 +13,9 @@ import type { FileEditorAction } from "#/types/agent-server/core/base/action";
 import type { FileEditorObservation } from "#/types/agent-server/core/base/observation";

 export const CANVAS_DEMO_CONVERSATION_ID = "canvas-demo";
-export const CANVAS_DEMO_FILE_PATH = "canvas.md";
+export const CANVAS_DEMO_FILE_PATH = "/workspace/project/deploy/Dockerfile";

-export const CANVAS_DEMO_MARKDOWN = [
-  "# Agent Canvas Demo",
-  "",
-  "This report was generated by the agent and opened from the file chip in chat.",
-  "",
-  "## Release snapshot",
-  "",
-  "| Area | Status | Next step |",
-  "| --- | --- | --- |",
-  "| Conversation UX | Ready | Validate the drawer flow |",
-  "| Markdown preview | Ready | Review rich rendering |",
-  "| Accessibility | Ready | Confirm keyboard navigation |",
-  "",
-  "## Highlights",
-  "",
-  "- The artifact stays in the conversation workspace.",
-  "- The chat chip is the compact entry point.",
-  "- The Files drawer provides the full rich Markdown view.",
-].join("\n");
+export const CANVAS_DEMO_MARKDOWN = "FROM node:22-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCOPY . .\nCMD [\"node\", \"server.js\"]";

 const BASE_TIME = Date.UTC(2026, 6, 29, 12, 0, 0);
 const timestamp = (offsetSeconds: number) =>
@@ -117,7 +99,7 @@ export const CANVAS_DEMO_EVENTS: OpenHandsEvent[] = [
   createMessage(
     "canvas-demo-user",
     "user",
-    "Create a concise project canvas as a Markdown report.",
+    "Create a Dockerfile in deploy/Dockerfile for the Node.js app.",
     0,
   ),
   createCanvasAction,
@@ -125,7 +107,7 @@ export const CANVAS_DEMO_EVENTS: OpenHandsEvent[] = [
   createMessage(
     "canvas-demo-agent",
     "assistant",
-    "I generated the project canvas. Open it from the file chip above.",
+    "Created deploy/Dockerfile.",
     3,
   ),
 ];
